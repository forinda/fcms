{
  "name": "@forinda-cms/render",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Spec → HTML. The deterministic runtime: no LLM at request time.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "@forinda-cms/spec": "workspace:*" },
  "devDependencies": { "typescript": "^7.0.2", "vitest": "^4.1.2" }
}
